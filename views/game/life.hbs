<div class="container">
	<h1>Start a new life in {{title}}</h1>
	<br>
	<p>You can be anyone you want to be.	Anything is possible.</p>
	<p>You will have <strong>{{game.turns}}</strong> turns to get as rich as you can.</p>
	<form method="POST" action="/game/life">
		<fieldset class="form-group">
			<label for="life-location">Select where you'd like to live</label>
			<select class="form-control" name="location" id="life-location">
				{{#each places}}
					<option value="{{id}}">{{city}}, {{country}} - {{continent}}</option>
				{{/each}}
			</select>
		</fieldset>

		<table class="table table-bordered table-sm">
			<tbody>
				<tr class="table-success">
					<td>Starting Cash</td>
					<td>$<strong>{{game.bank.starting_cash}}</strong></td>
				</tr>
				<tr class="table-danger">
					<td>Starting Debt</td>
					<td>$<strong>{{game.bank.starting_debt}}</strong></td>
				</tr>
			</tbody>
		</table>

		<button id="life-start-btn" type="submit" class="btn btn-primary">Start your life!</button>
	</form>
</div><!-- /.container -->
